// Code generated by cmd/mcdata-gen; DO NOT EDIT.
package {{ .Package }}

import "github.com/go-theft-craft/minecraft-protocol/data"

type soundRegistry struct { byID map[data.SoundID]data.Sound; byName map[string]data.Sound; all data.Sounds }

func newSoundRegistry() data.SoundRegistry {
	all := data.Sounds{
		{{- range .Data }}
		{ID: {{ .ID }}, Name: {{ printf "%q" .Name }}},
		{{- end }}
	}
	byID := make(map[data.SoundID]data.Sound, len(all)); byName := make(map[string]data.Sound, len(all))
	for _, value := range all { byID[value.ID] = value; byName[value.Name] = value }
	return &soundRegistry{byID: byID, byName: byName, all: all}
}

func (registry *soundRegistry) ByID(id data.SoundID) (data.Sound, bool) { value, ok := registry.byID[id]; return value, ok }
func (registry *soundRegistry) ByName(name string) (data.Sound, bool) { value, ok := registry.byName[name]; return value, ok }
func (registry *soundRegistry) All() data.Sounds { return registry.all.Clone() }
