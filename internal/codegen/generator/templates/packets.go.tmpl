{{/* Packet declarations now come from internal/codegen/packetgen. This
template remains embedded so older template inventories fail closed instead
of silently restoring reflection-tag-only packets. */}}
